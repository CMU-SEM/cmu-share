# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
pod 'Firebase/Core'
pod 'Firebase/Auth'

target 'cmu-share' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for cmu-share

  target 'cmu-shareTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'cmu-shareUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
